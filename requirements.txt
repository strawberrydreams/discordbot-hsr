# Lock regeneration: uv pip compile requirements.txt --universal --no-strip-markers --output-file requirements.lock
discord.py==2.7.1
aiohttp==3.14.3 # webadmin_cog가 직접 쓴다. discord.py 전이 의존성에 기대지 않는다
python-dotenv==1.2.3
openai==2.54.0 # 3.x는 HTTPX2 transport migration을 별도 검증한 뒤 올린다
google-genai==2.20.0
enka==2.5.20 # GPL-3.0. 이 의존성 때문에 저장소 전체가 GPL-3.0이다
